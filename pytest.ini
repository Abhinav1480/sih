[pytest]
pythonpath = backend
asyncio_mode = auto
markers =
    honesty: enforces docs/HONESTY_RULE.md. Run with `pytest -m honesty`.
    honesty_proof: proves an honesty check fails on its violation. `pytest -m honesty_proof`.
